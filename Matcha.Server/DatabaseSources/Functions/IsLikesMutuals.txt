CREATE FUNCTION IsLikesMutuals (
	first_id BIGINT,
	second_id BIGINT
) RETURNS BIT

BEGIN

	RETURN EXISTS((SELECT * FROM likes WHERE likes.who = first_id AND likes.whom = second_id))
	   AND EXISTS((SELECT * FROM likes WHERE likes.who = second_id AND likes.whom = first_id));

END