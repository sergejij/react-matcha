CREATE TABLE dislikes (
	who BIGINT NOT NULL,
	whom BIGINT NOT NULL,

	PRIMARY KEY(who, whom),
	FOREIGN KEY(who) REFERENCES user_data(id) ON DELETE CASCADE,
	FOREIGN KEY(whom) REFERENCES user_data(id) ON DELETE CASCADE
);