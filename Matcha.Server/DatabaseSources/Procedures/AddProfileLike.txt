CREATE PROCEDURE AddProfileLike (
	liker_id BIGINT,
	liked_id BIGINT
)

BEGIN

	IF NOT EXISTS(SELECT * FROM likes WHERE likes.liker_id = liker_id AND likes.liked_id = liked_id)
	THEN
		INSERT INTO likes (liker_id, liked_id) VALUES (liker_id, liked_id);
	END IF;

END