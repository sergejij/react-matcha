CREATE PROCEDURE AddProfileVisiter (
	visiter_id BIGINT,
	visited_id BIGINT
)

BEGIN

	IF NOT EXISTS(SELECT * FROM visits WHERE visits.visiter_id = visiter_id AND visits.visited_id = visited_id)
	THEN
		INSERT INTO visits (visiter_id, visited_id) VALUES (visiter_id, visited_id);
	END IF;

END