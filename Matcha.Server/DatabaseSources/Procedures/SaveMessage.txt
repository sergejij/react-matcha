CREATE PROCEDURE SaveMessage(
	from_id BIGINT,
	to_id BIGINT,
	content TEXT,
	readed BIT
)

BEGIN

	INSERT INTO messages (
		from_id,
		to_id,
		content,
		readed
	) VALUES (
		from_id,
		to_id,
		content,
		readed
	);

END